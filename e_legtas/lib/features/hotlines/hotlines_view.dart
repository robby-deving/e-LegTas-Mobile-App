import 'package:flutter/material.dart';

class HotlinesView extends StatelessWidget {
  const HotlinesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Hotlines Page'),
    );
  }
}
